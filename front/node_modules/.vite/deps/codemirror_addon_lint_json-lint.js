import {
  require_codemirror
} from "./chunk-B6V2QL5N.js";
import {
  __commonJS
} from "./chunk-TFWDKVI3.js";

// node_modules/codemirror/addon/lint/json-lint.js
var require_json_lint = __commonJS({
  "node_modules/codemirror/addon/lint/json-lint.js"(exports, module) {
    (function(mod) {
      if (typeof exports == "object" && typeof module == "object")
        mod(require_codemirror());
      else if (typeof define == "function" && define.amd)
        define(["../../lib/codemirror"], mod);
      else
        mod(CodeMirror);
    })(function(CodeMirror2) {
      "use strict";
      CodeMirror2.registerHelper("lint", "json", function(text) {
        var found = [];
        if (!window.jsonlint) {
          if (window.console) {
            window.console.error("Error: window.jsonlint not defined, CodeMirror JSON linting cannot run.");
          }
          return found;
        }
        var jsonlint = window.jsonlint.parser || window.jsonlint;
        jsonlint.parseError = function(str, hash) {
          var loc = hash.loc;
          found.push({
            from: CodeMirror2.Pos(loc.first_line - 1, loc.first_column),
            to: CodeMirror2.Pos(loc.last_line - 1, loc.last_column),
            message: str
          });
        };
        try {
          jsonlint.parse(text);
        } catch (e) {
        }
        return found;
      });
    });
  }
});
export default require_json_lint();
//# sourceMappingURL=codemirror_addon_lint_json-lint.js.map
